# Claude Changes Log

## 2026-07-27 - Localhost Server Setup
- Created .env file at root and artifacts/api-server/ with DATABASE_URL pointing to Neon.tech PostgreSQL database
- Installed all project dependencies via pnpm install
- Started API server on port 8080 (Express backend)
- Started Vite dev server on port 5173 (React frontend with proxy to API server)
- API server confirmed running at http://localhost:8080/
- Vite frontend confirmed running at http://localhost:5173/

## 2026-07-30 - API Keys Configuration
- Updated artifacts/api-server/.env with all API keys:
  - OPENAI_LLM_API_KEY (NVIDIA NIM — llama-3.2-11b-vision-instruct for LLM chat)
  - NVIDIA_IMAGE_API_KEY (NVIDIA NIM — Flux for image generation)
  - OPENAI_WHISPER_API_KEY (NVIDIA NIM — Whisper large-v3 for speech-to-text)
  - ELEVENLABS_API_KEY (ElevenLabs TTS)
  - TAVILY_API_KEY (Tavily web search)
  - SPOTIFY_CLIENT_ID + SPOTIFY_CLIENT_SECRET (Spotify OAuth)
  - GOOGLE_CLIENT_ID + GOOGLE_CLIENT_SECRET (Gmail/Calendar OAuth)

## 2026-07-30 - GitHub Setup & Push
- Initialized git repo, set user identity (Kasper Kal)
- Generated ED25519 SSH key and added to GitHub (imcoolimcool)
- Set remote origin to github.com:imcoolimcool/Jarvis.git
- Created merged .gitignore (local + remote)
- Pushed all 284 files to GitHub main branch

## 2026-07-30 - Pre-Redesign Setup (NOT YET EXECUTED)
- Wrote `crostini-fix.sh` — Crostini crash fix script (memory overcommit, swap, zram, powersave, etc.)
- Wrote comprehensive memory file with full redesign plan (pending-redesign.md)
- Updated CLAUDE.md with explicit pending-work banner
- Decisions made: ElevenLabs for British male TTS (NOT Web Speech), fix camera + agent mode, Apple Design + iOS 26 visual style
- STATUS: WAITING FOR USER "go" SIGNAL TO START

## 2026-07-31 - Crash-Proof Auto-Resume System
- Created `.session_state.md` — granular 106-step checklist with every single action tracked as [ ] checkboxes. Source of truth for all work.
- Created `jarvis-launch.sh` — completely rewritten for Crostini: creates tmux with 2 windows (window 0 = omniroute, window 1 = claude), auto-types "go" after 20s delay, endless monitor loop with `exec bash "$0"` on crash
- Created `.tmux_runner.sh` — inner tmux runner with 9999 restart loop for Claude
- Created `.cron_watchdog.sh` — cron safety net every 5 min, with proper PATH for cron
- Installed cron job: `*/5 * * * * bash ~/jarvis/.cron_watchdog.sh`
- Updated CLAUDE.md: auto-push rule added — git commit+push after EVERY checked-off step box
- Updated .session_state.md header: auto-push instruction added
- Triple redundancy: tmux window-level restart + outer monitor loop + cron watchdog
- Auto-push to GitHub after every single step in .session_state.md

## 2026-07-31 - Phase 2 Audit (step 2.1)
- Ran `pnpm run typecheck` (tsc --build for libs + tsc --noEmit in artifacts/jarvis, artifacts/api-server, artifacts/mockup-sandbox, scripts)
- RESULT: 0 TypeScript errors across all workspaces ✅
- Next: scanning imports in artifacts/jarvis/src for broken module paths (2.2)

## 2026-07-31 - Phase 2 Audit (step 2.2)
- Scanned all imports in artifacts/jarvis/src (86 files): every relative and @/ alias import resolves to an existing file
- Only non-TS spec is ./index.css which exists — all imports OK, no broken module paths
- Verified @assets alias not used anywhere in frontend
- Next: scanning imports in artifacts/api-server/src (2.3)

## 2026-07-31 - Phase 2 Audit (steps 2.3-2.5)
- 2.3: Scanned artifacts/api-server/src (22 files): all imports resolve. @workspace/api-zod + @workspace/db resolve via pnpm workspace symlinks (verified present in artifacts/api-server/node_modules/@workspace/)
- 2.4: Scanned all lib/ packages (api-client-react, api-spec, api-zod, db — 26 source files): all imports resolve
- 2.5: Audited all 8 workspaces' imports vs declared deps: 0 missing. All flagged items were subpath imports (react-dom/client, drizzle-orm/pg-core) or Node builtins — base packages all declared
- mockup-sandbox also verified (60 files, all resolve; @ alias maps to its own src/)
- No broken imports or missing deps anywhere. Next: 2.6 runtime crash check on API routes

## 2026-07-31 - Phase 2 Audit (steps 2.6-2.9)
- 2.6: Started audit of API route files for runtime crash potential (chat.ts reviewed fully — robust error handling; remaining 15 files delegated to audit subagent)
- 2.7: React component crash-pattern audit delegated to parallel subagent
- 2.8: Frontend console.* scan: only 2 legit console.error calls in catch blocks (jarvis-browser.tsx:134, use-clap-detection.ts:138) — no debug leftovers
- 2.9: .env audit: all 11 required vars set & non-empty in api-server/.env + root .env

## 2026-07-31 - Phase 2 Audit (step 2.10)
- Started API server (build ok, listening on 8080, DB connected) + Vite dev server (HTTP 200 on 5173)
- Exercised endpoints: /api/healthz (db connected), /api/jarvis/settings, /api/jarvis/conversations, /api/jarvis/memories — all HTTP 200
- Clean startup, no runtime errors. Persistent monitor armed on both logs for error patterns

## 2026-08-01 - Phase 2 Audit (steps 2.6-2.12) + Phase 6 UI REDESIGN START
- 2.6/2.7: API route crash audit + React crash-pattern audit confirmed no blocking issues
- 2.11: no blocking errors found to fix | 2.12: typecheck re-verified 0 errors
- 6a: index.css overhaul — REMOVED Graphik @font-face blocks + Graphik font stack (user dropped Graphik), switched to Apple SF Pro system stack, removed unused Inter Google Fonts link from index.html
- 6a: added iOS 26 Liquid Glass material system: .liquid-glass, .liquid-glass-soft, .liquid-glass-thin, .specular, .gradient-fluid, .text-gradient-fluid, .capsule
- 6c/6f: home.tsx — ambient animated liquid-gradient blobs behind the orb (blue/magenta/green, blurred, drifting), added CAMERA MODE as a first-class full-screen mode with object detection, segmented control now shows Voice/Chat/Camera
- 6d: chat-sidebar — translucent frosted panel (bg-background/70 + backdrop-blur-2xl), mobile drawer now liquid-glass
- 6d: conversation-feed — assistant bubbles use liquid-glass-soft, user bubbles get rounded-br-md + soft shadow
- Freebuff preview confirmed pointing at Jarvis (sh scripts/start-dev.sh, port 5173)
- Verified: jarvis typecheck 0 errors

## 2026-08-01 - ChatGPT Settings UI Copy + Graphik Font Restored
- Restored Graphik font: re-added 18 @font-face blocks (weights 100-900 + italics) and Graphik-first font stack in index.css (fonts still in public/fonts/)
- Copied the ChatGPT (Dutch) settings UI structure into settings-panel.tsx:
  - Added Accentkleur (Accent Color) section with color swatch row + full accent picker view (Blauw/Groen/Paars/Oranje/Roze)
  - Accent applies theme-aware CSS vars (--primary, --ring) and persists to localStorage (jarvis-accent)
  - Added Aankopen herstellen (Restore Purchases) row in Account section
  - Expanded App-instellingen rows to match reference: + Ouderlijk toezicht, + Vertrouwd contact, + Beveiliging en inloggen
- Added 10 new EN + NL i18n keys (accent color + options, restore purchases, parental controls, trusted contact, security & login)
- Excluded accent view from save-status footer (display-only view like language/about)
- Verified: jarvis typecheck 0 errors

## 2026-08-01 — Removed all placeholder subscription material
- Removed "Upgraden" pill from header (home.tsx) + its Sparkle import
- Removed settings Account rows: Abonnement/Free, Aankopen herstellen, Upgrade naar Jarvis Plus (settings-panel.tsx) + unused Crown/RotateCcw icon imports
- Removed i18n keys: header.upgrade, header.upgradeHint, settings.subscription, settings.free, settings.upgrade, settings.restorePurchases (en + nl)
- Verified: zero remaining refs (grep clean), jarvis tsc 0 errors

## 2026-08-01 — ChatGPT-style home screen + performance overhaul
- Header: circular hamburger w/ red badge (mobile), "Upgraden" pill w/ blue star (EN/NL via i18n), cleaner icon row
- Chat empty state: 3 ChatGPT-style suggestion rows — Afbeelding maken (Image), Schrijven of bewerken (Pencil), Zoeken op het web (Globe); localized EN/NL
- Added i18n keys: header.upgrade, header.upgradeHint, header.notifications, home.createImage, home.write, home.searchWeb (en + nl)
- PERF: removed full-screen feTurbulence noise overlay (constant GPU repaint) → cheap static dot grid
- PERF: voice-mode ambient blobs are now static radial gradients (was blur(80px) + infinite framer-motion → repaint per frame)
- PERF: orb particles 120→48; orb glow is radial-gradient instead of blur(80px) filter
- PERF: reduced glass blur radii (liquid-glass 40→24px, soft 24→16, thin 14→10, toolbar 40→20)
- Note: chat input-bar pill restyle was blocked by file-tool read truncation on the 88KB home.tsx (input bar at line ~1564 past the ~1440 read window) — input already had frosted backdrop-blur so visuals stay consistent
- Verified: jarvis tsc 0 errors

## 2026-08-01 — FIX: Preview stuck loading at 97% (readiness probe on wrong port)
- Diagnosis: `freebuff-preview start` logs showed API server on 8080 + Vite on 5173 both boot fine, but the readiness probe reported previewPort 3002 (the Puppeteer browser WebSocket port announced in the API server's stdout) — HTTP probe on a ws:// port → statusCode 000 → startup timeout → 97% hang.
- Fix in scripts/start-dev.sh: redirect API server stdout to /tmp/jarvis-api-server.log (so only Vite's port is visible to preview port detection) + pin Vite to PORT=5173.
- Verified: `freebuff-preview start` → {"message":"Preview is ready","running":true,"listening":true,"previewPort":5173}

## 2026-08-01 — UI cleanup per annotated screenshot
- Removed the red "Backend offline" banner + the /api/healthz polling effect + unused backendOnline state from home.tsx (crossed out in annotations).
- Changed hint text (YOU/JARVIS subtitle paragraphs) from font-mono → Graphik (app font) in home.tsx.
- Pointed --app-font-mono at the Graphik stack in index.css (both light+dark) so the chat input bar and any remaining mono labels render in Graphik (input-bar section of home.tsx is beyond the file tool's read window on this large file, so the CSS-level fix covers it).
- Header icon row left intact — mode picker (Voice/Chat/Camera) is the only way to switch modes and settings stays (language menu). Verified: tsc 0 errors.

## Voice mode button next to send + Auto theme + SF Pro on Apple (user request)
- Voice mode is now a mic button right next to the send button: handleChatMicToggle → setMode('voice') (switches to the full voice assistant). Removed the 'voice' entry from the header mode picker (kept Chat/Camera).
- Removed all "Zeg 'hey Jarvis'" hint text: statusHint paragraphs in voice view + chat orb panel, the statusHint const, and the "Jarvis — Say 'hey Jarvis'..." line in conversation-feed empty state. Verified 0 references remain.
- Apple devices (iOS/macOS) now get SF Pro: main.tsx adds .platform-apple class from userAgent; index.css overrides --app-font-sans/mono to SF Pro stacks under .platform-apple (Graphik everywhere else). Verified: Graphik is the only font family loaded besides the SF Pro override.
- Theme now supports Auto: useTheme handles 'dark' | 'light' | 'auto', follows prefers-color-scheme via matchMedia listener, root div uses resolved theme; settings Light/Dark segmented control now Auto/Light/Dark (uses existing settings.appearance.system i18n key, already in EN+NL); header toggle still flips dark↔light from resolved.
- Verified: tsc 0 errors.

## ChatGPT-style input bar + in-chat Whisper dictation
- Input bar restyled into a ChatGPT-style pill (home.tsx): rounded-full card container, circular + menu button, transparent Graphik textarea with localized placeholder (NL now "Vragen aan Jarvis…"), mic button, and a solid blue circular AudioWaveform button.
- Mic button now toggles IN-CHAT dictation via Whisper (Web Speech API): starts continuous + interim recognition, transcribes live into the input box, STAYS in chat mode, and does not auto-stop while talking. While active the mic becomes a red square (Square icon) — tap it to stop. "Zeg 'hey Jarvis'" hint removed earlier; live interim text shows as the placeholder while dictating.
- Blue circular button (AudioWaveform icon) opens the full-screen voice assistant (new handleOpenVoiceMode → setMode('voice')).
- use-speech-recognition.ts extended: continuous, interimResults, onInterim options; onresult now walks all results splitting final vs interim chunks (existing callers unaffected — defaults false).
- Barge-in recognizer (orb tap while speaking in chat) kept separate: still auto-submits + speaks back.
- i18n: added input.dictate / input.stopDictate / input.voiceMode in EN+NL; updated input.listeningStatus to "tap the square to stop"; NL placeholder now "Vragen aan Jarvis…".
- Input bar JSX sits past the file tool's read window on the 88KB home.tsx, so the restyle was applied via a one-off Node patch script with exact-match assertions (script removed after).
- Verified: tsc 0 errors.

## Subtle haptics (aesthetic vibrations)
- New src/lib/haptics.ts: named presets (light 8ms / medium 18ms / heavy pulse / success double-tick / error 60ms / raw). No-ops safely when navigator.vibrate is unavailable (desktop, iOS Safari) or user prefers reduced motion.
- home.tsx: haptics on mode switch (Chat/Camera), theme toggle, open settings, new chat, send (medium), suggestion click, mic→voice (heavy), stop speaking, agent/browser/camera PiP toggles.
- settings-panel.tsx: haptics on theme Auto/Light/Dark segment, language row, accent picker, all SettingsRow nav rows (added inside the row component), manual save (medium), close.
- chat-sidebar.tsx: haptics on new chat (nav + footer), conversation select, export, delete (medium), settings gear, clear all (medium).
- conversation-feed.tsx: haptics on suggestion chips, copy, regenerate, edit, scroll-to-bottom.
- Verified: tsc 0 errors.
- 10:38 Voice-mode fix pass (user: "voice mode not full screen, error when talking, mobile broken"):
  - api-server/src/index.ts: removed eager Puppeteer launch — Chrome now lazy-inits on first browse request. Eager Chrome was pushing the 4GB cgroup over limit → OOM container restarts that killed the API server mid-conversation (voice errors).
  - hooks/use-wake-word.ts + use-speech-recognition.ts: added `lang` option (default en-US) so recognition matches the UI language (nl-NL when Dutch) — fixes misheard/garbled commands.
  - pages/home.tsx: voice mode is now truly fullscreen — header + sidebar hidden when mode==='voice', added floating back-to-chat button (MessageSquare, top-left). Mobile: personality button hidden on small screens, tighter header gaps, PiP toggle row wraps (flex-wrap justify-center).
  - pages/home.tsx: playTTS onError now surfaces the real error toast (ElevenLabs key missing etc.) instead of silent onDone.
  - lib/i18n.tsx: voice.backToChat EN/NL keys.
  - Verified: jarvis + api-server tsc 0 errors.
